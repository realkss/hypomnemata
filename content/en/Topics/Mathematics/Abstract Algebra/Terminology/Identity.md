An **identity** for a binary operation is an element `e` such that

`e*a = a*e = a`

for every element `a` in the set.

If a two-sided identity exists, it is unique.

## One-line intuition

The identity is the do-nothing element for the operation.
