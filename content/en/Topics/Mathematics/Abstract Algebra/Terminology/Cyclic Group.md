A **cyclic group** is a group generated by a single element $g$:

$G = \langle g \rangle$.

That means every element of $G$ is a power of $g$.

Every cyclic group is abelian, and every subgroup of a cyclic group is cyclic.

## One-line intuition

A cyclic group is a group you can get by repeatedly applying one element and its inverse.
