A **subgroup** of a group `G` is a subset `H` that is itself a group under the same operation.

The fastest subgroup criterion is:

`H` is a subgroup if it is nonempty and `xy^{-1} in H` for all `x,y in H`.

## One-line intuition

A subgroup is a smaller group already living inside a bigger one.
