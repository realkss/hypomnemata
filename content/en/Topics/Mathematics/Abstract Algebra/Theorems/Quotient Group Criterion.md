Let `H <= G`. The multiplication rule

`(gH)(kH) = gkH`

defines a group structure on the set of cosets if and only if `H` is normal in `G`.

## Why it matters

This is the exact condition that makes quotient groups possible.
