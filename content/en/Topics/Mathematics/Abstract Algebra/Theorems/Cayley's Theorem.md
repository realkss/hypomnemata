**Cayley's theorem** states that every group is isomorphic to a subgroup of some symmetric group.

The standard proof sends `g in G` to the permutation `L_g(x)=gx`.

## Why it matters

It says every abstract group can be realized concretely as permutations.
